/// @description Insert description here
// You can write your code in this editor
if(hit && floor((alarm[0] - 1)/10) mod 2 = 0 )
{
shader_set(shd_white_sprite);
	draw_self();
shader_reset();
}
else
{
	draw_self();	
}